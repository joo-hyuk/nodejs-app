language: generic

sudo: required

services:
  - docker

before_install:
  - docker build -t maxx09/react-test-app -f ./frontend/Dockerfile.dev ./frontend

script:
  - docker run -e CI=true maxx09/react-test-app npm run test

after_success:
  - docker build -t maxx09/docker-frontend ./frontend
  - docker build -t maxx09/docker-backend ./backend
  - docker build -t maxx09/docker-nginx ./nginx

  - echo "$DOCKER_HUB_PASSWORD" | docker login -u "$DOCKER_HUB_ID" --password-stdin

  - docker push maxx09/docker-frontend
  - docker push maxx09/docker-backend
  - docker push maxx09/docker-nginx